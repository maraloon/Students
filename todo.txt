-настроить права для папок 
-в классе Student все свойства public. Возможно их стоит закрыть и сделать getter's/setter's
-для ссылки Выход сделать token
+удалить вторую таблицу и наверное, обойтись без пароля, только хеш

+токен на регистрации
+$name = isset($_POST['name']) ? strval($_POST['name']) : '';
-дописать StudentValidator (регулярки)
+StudentValidator проверка типа
+StudentDataGateway->addStudent
+StudentDataGateway->errors
-StudentDataGatewayСделать проверку, а нет ли такого чувака в базе по e-mail'у

-StudentValidator: вместо bool enum:  'is_resident'=>array('enum',array('resident','foreign'))

+как читать данные из JSON
+заполнить JSON-конфиг
+StudentDataGateway - настроить подключение к БД
+вывести хотя бы строчку из БД
+вывести все строки таблицы
+написать представление (согласно заданию)
+github-консоль
+залить на проверку